# 项目：安平丝网 AI 客服 SaaS 平台（KaaS）

> 注意：本文件为 Cursor 专用入口。通用版规则见 `.qwen/KAAS_RULES.md`（适用于所有 AI CLI 工具）。

## 一句话描述
为传统制造业（首先安平丝网产业集群）提供 AI 岗位能力托管 SaaS 服务。
商家零部署零运维，按月付费，我们托管一切。

## 当前阶段
MVP Demo 验证期——丝网报价小助手，用 FastGPT + 本地代码（报价/话术引擎）最快跑通闭环。

## 完整规则与技能
**本文件仅保留快速入口，完整规则请查阅：**
- `.qwen/KAAS_RULES.md` - 通用 Rules（项目约束）
- `.qwen/KAAS_SKILL.md` - 通用 Skill（工作流编排）

## 技术栈
### Demo 阶段（当前）
- AI 平台：FastGPT 在线版（cloud.fastgpt.cn）
- 大模型：DeepSeek-Chat API（推理）
- 消息路由：Python + FastAPI（`msg-router/`，端口 8000）
- RPA：千牛（Python + uiautomation）+ 拼多多（Playwright）
- 本地开发：Windows 本地，无需云服务器

### 正式版（Demo 验证后）
- AI 平台：Dify（Docker 私有部署）+ PostgreSQL + Redis + Weaviate
- 计费网关：Python 3.11 + FastAPI + PostgreSQL + Redis
- 部署方式：Docker Compose 一把梭

## 代码仓库结构
D:\MyProject\kaas-platform\
├── msg-router/              # 消息路由服务（FastAPI，Demo + 正式版共用）
├── rpa-qianniu/             # 千牛 RPA（Python + uiautomation）
├── rpa-pdd/                 # 拼多多 RPA（Playwright + FastAPI）
├── scripts/                 # 运维脚本
├── .qwen/                   # AI CLI 通用规则与技能
│   ├── KAAS_RULES.md        # 通用 Rules（CLI 工具无关）
│   └── KAAS_SKILL.md        # 通用 Skill（工作流编排）
└── .cursor/                 # Cursor 专用配置（兼容层）
    ├── .cursorrules         # 本文件
    └── rules/               # Cursor 专用规则

## 协作架构
- **Nano Auto**（Notion AI Agent）= 大脑：拆任务、写 Spec、验收、管文档
- **Runner**（AI 编程工具）= 工程师：按 Spec 写代码（Cursor/Qwen CLI/Claude Code 等）
- **David**（创始人）= 决策者：创建任务、验收确认

## 任务流转状态机
编辑中 → 待启动 → Nano规划 → Runner开发 → Nano验收 → David验收 → 已完成

## 硬性约束（所有任务通用）
1. 所有密码/密钥从环境变量读取，禁止硬编码
2. DB 用 UTC 存储时间；业务层按 Asia/Shanghai 计算
3. 必须输出完整可运行代码，禁止省略号和「自行补充」
4. 每个文件输出完整内容
5. 不确定的地方列为 Open Questions，不要静默猜测
6. 关键路径禁止伪代码
7. **详细规则请查阅 `.qwen/KAAS_RULES.md`**