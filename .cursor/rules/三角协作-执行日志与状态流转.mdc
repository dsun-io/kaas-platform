---
description: 三角协作执行器规则（新版状态机与选项卡日志）
alwaysApply: true
---

# 三角协作执行器（Cursor）规则

## 1) 角色与真相源
- 仅存在三个角色：`Nano Auto`、`Cursor`、`David`；规则中禁止出现 `QClaw`。
- Notion 是唯一真相源；执行时以任务卡正文 `📋 任务 Spec` 为唯一输入依据。
- `三角协作工作流规范` 页面只读，仅查看，不写入。

## 2) 任务页面结构（必须遵守）
- 属性用于筛选：`任务名`、`状态`、`优先级`、`关联模块`、`任务类型`、`产出物`、`验收结果`、`创建时间`、`最后更新`。
- 正文固定 3 个选项卡：
  - `📋 任务 Spec`：Nano 维护需求、方案、验收标准、执行指令
  - `📝 执行日志`：Nano/David 执行记录
  - `🔧 Runner 执行日志`：Cursor 开发执行记录（Cursor日志写入此选项卡）
- 页面底部独立区块：`🔍 David 验收反馈`。

## 3) 完整状态列表（7 个）
- `编辑中`（灰，to_do）
- `待启动`（橙，to_do）
- `Nano规划`（蓝，in_progress）
- `Cursor开发`（紫，in_progress）
- `Nano验收`（蓝，in_progress）
- `David验收`（红，to_do）
- `已完成`（绿，complete）

## 4) 状态前置校验表（严格执行）
- `待启动` <- `编辑中`
- `Nano规划` <- `待启动 / David验收 / Cursor开发`
- `Cursor开发` <- `Nano规划 / Nano验收 / David验收`
- `Nano验收` <- `Cursor开发`
- `David验收` <- `Nano验收 / Nano规划`
- `已完成` <- `David验收`
- 非法流转必须拒绝，并在 `🔧 Runner 执行日志` 追加：`[角色] YYYY-MM-DD HH:mm 非法状态流转：A -> B（已拒绝）`。

## 5) Cursor 执行规则
- 启动前读取 `📋 任务 Spec`，不得按猜测扩需求。
- 完成后必须按顺序做四件事（顺序不可变）：
  - 先执行 `git add`、`git commit`、`git push`
  - push 成功后更新属性 `产出物`（PR/分支/commit）
  - 向 `🔧 Runner 执行日志` 选项卡追加结构化记录（必须完整包含以下12项骨架，缺一不可）
  - 状态从 `Cursor开发` 流转到 `Nano验收`
- 阻塞时在 `🔧 Runner 执行日志` 写明原因，并将状态回退到 `Nano规划`。

## 6) 执行日志规范（写入 `🔧 Runner 执行日志` 选项卡，必须高密度可审计）
- 格式：`[Nano/Cursor/David] YYYY-MM-DD HH:mm 内容`
- **只允许追加，不覆盖历史** - 必须使用 `notion-update-page` 工具的 `update_content` 命令追加，**绝对禁止**使用 `replace_content`。
- Cursor 日志必须包含以下 12 项（缺一不可）：
  - `任务元数据`：task_id、task_url、spec_version、repo、branch、pr_or_commit、operator
  - `目标与范围`：本次做什么/不做什么，边界条件
  - `输入快照`：读取了哪些 Notion 区块与关键字段
  - `环境快照`：关键命令版本/运行环境摘要（如与任务相关）
  - `执行时间线`：按序号记录每一步动作、原因、结果
  - `改动明细`：每个文件的改动点、目的、影响面
  - `验证矩阵`：命令、预期、实际、结论、证据摘要
  - `验收映射`：验收标准 -> 证据 -> 结论
  - `异常与处置`：失败/重试/降级策略与最终状态
  - `风险与回滚`：风险触发条件、监控信号、回滚步骤
  - `产出清单`：PR/分支/文件路径/制品链接
  - `待办与建议`：后续动作、人工确认项、依赖方动作
- 推荐使用以下骨架（追加到 `🔧 Runner 执行日志`）：
```markdown
[Cursor] YYYY-MM-DD HH:mm 开发完成
## 1. 任务元数据
- task_id:
- task_url:
- spec_version:
- repo/branch:
- pr_or_commit:
- operator:

## 2. 目标与范围
- in_scope:
- out_of_scope:
- constraints:

## 3. 输入快照
- notion_spec:
- acceptance_items:
- current_status:

## 4. 执行时间线
1) action -> reason -> result
2) ...

## 5. 改动明细
- file: <path>
  - change:
  - purpose:
  - impact:

## 6. 验证矩阵
- cmd:
  - expected:
  - actual:
  - result: PASS/FAIL
  - evidence:

## 7. 验收映射
- criterion -> evidence -> PASS/FAIL

## 8. 异常与处置
- issue:
- handling:
- final:

## 9. 风险与回滚
- risk:
- trigger:
- rollback:

## 10. 产出清单
- artifact:

## 11. 待办与建议
- next_action:
```

## 7) David验收打回路由与全局约束
- 打回路由：
  - Spec 问题 -> `Nano规划`
  - 实现问题 -> `Cursor开发`
- 单任务限制：同一时间仅允许一个活跃任务（`Nano规划 / Cursor开发 / Nano验收`）。
- 循环上限：`Cursor开发 <-> Nano验收` 超过 3 次，需升级到 `David验收` 并记录人工介入。

## 8) Cursor 执行铁律（提交前必做，不可跳过）

> **铁律一：强制自检（提交前必做，不可跳过）**
> 开发完成、提交验收之前，必须逐项对照任务 Spec 输出自检清单。格式：Spec编号 → 修改的文件 → commit 中的具体改动 → PASS/SKIP/FAIL。存在 FAIL 必须先修复再提交。SKIP 必须写原因。没有自检清单 = Nano 验收直接打回。

> **铁律二：强制详细日志且禁止删除历史日志**
> 每次写入 `🔧 Runner 执行日志` 必须完整包含 12 项骨架：任务元数据、目标与范围、输入快照、执行时间线、改动明细、验证矩阵、验收映射、异常与处置、风险与回滚、产出清单、待办与建议。**严禁删除、覆盖、截断之前的任何日志内容**，只允许在末尾追加。缺任一项 = Nano 验收直接打回。

> **铁律三：强制推送远程仓库**
> 所有开发完成后必须执行 `git push` 将所有 commit 推送到远程仓库。未推送到远程 = 任务未完成，不允许改状态为 Nano验收。产出物字段必须包含可在 GitHub 上验证的 commit hash 或 PR 链接。本地 commit 不算交付。违反 = Nano 验收直接打回。

> **铁律四：强制使用 MCP 更新 Notion**
> **严禁创建任何 Python 脚本、PowerShell 脚本或其他外部工具来更新 Notion。** 所有 Notion 页面更新（包括执行日志、状态流转、属性更新）必须且只能使用 Cursor 内置的 MCP 工具（`user-Notion` 服务器）。
> 
> ✅ **正确做法**：使用 `CallMcpTool` 调用 `notion-update-page`、`notion-fetch` 等工具
> ❌ **禁止做法**：创建 `scripts/update_notion.py`、`scripts/create_task.py` 等脚本
> 
> 违反 = 立即删除脚本，重新使用 MCP 方式更新。

> **铁律五：强制 12 项完成后才流转状态**
> 未完成 12 项日志骨架（任务元数据、目标与范围、输入快照、执行时间线、改动明细、验证矩阵、验收映射、异常与处置、风险与回滚、产出清单、待办与建议、附录证据索引）、未执行 git push、未输出自检清单，一律禁止将状态从 `Cursor开发` 改为 `Nano验收`。违反 = 必须回退并补全。

> **铁律六：强制使用 update_content 追加日志，绝对禁止 replace_content**
> **严禁使用 `replace_content` 命令更新 Notion 页面内容**，该命令会删除/覆盖所有历史日志，严重违反铁律二。
> 
> ✅ **正确做法**：使用 `update_content` 命令，通过 `old_str` 定位已有内容（如 `"\t</tab>"` 或 `"YYYY-MM-DD HH:mm"`），`new_str` 在末尾追加新日志
> ❌ **绝对禁止**：使用 `replace_content` 替换整个页面内容
> 
> 违反 = 立即恢复被删除的日志，重新使用 update_content 追加，并接受处罚。

## 9) Cursor 异常援助任务规范

### 任务卡片属性

1. **任务类型**：`Cursor 异常援助`
2. **优先级**：`P1`（仅当阻塞整个迭代时可为 `P0`）
3. **任务名**：`[Cursor异常援助] <问题概述>`
4. **📋 任务 Spec 内容**：
```markdown
## 异常描述
- 发现时间：<YYYY-MM-DD HH:mm>
- 发现方式：<测试/代码审查/运行时>
- 影响模块：<模块名>
- 尝试次数：<如果是简单问题升级，记录已尝试次数>

## 错误详情
### 异常类型
<class 'ExceptionType'>

### 错误信息
```
<完整的错误日志/堆栈跟踪>
```

### 运行时上下文
- 执行的命令：<触发异常的命令>
- 环境状态：<相关环境信息>
- 相关配置：<相关配置文件内容>

## 复现步骤
1. <步骤1>
2. <步骤2>

## 预期行为
<正常应该发生什么>

## 实际行为
<实际发生了什么>

## 已尝试的修复（如适用）
### 第1次尝试
- 尝试方案：<做了什么>
- 结果：<成功/失败，失败原因>

### 第2次尝试
- 尝试方案：<做了什么>
- 结果：<成功/失败，失败原因>

## 相关日志
```
<完整的运行日志>
```

## 建议方向（供Nano参考）
- <可能的解决方向1>
- <可能的解决方向2>
```
